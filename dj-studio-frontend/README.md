# DJ Studio — UI
Interfaz futurista lista para correr con `npm install` + `npm run dev` dentro de `dj-studio-frontend`.
Modo **Local** funciona sin backend (sube audios y usa SpeechSynthesis para la voz del DJ).
Modo **Spotify** requiere conectar OAuth desde tu backend (Netlify Functions).
