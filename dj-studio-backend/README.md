# Backend (Netlify)
- Añade variables de entorno en Netlify:
  - SPOTIFY_CLIENT_ID
  - SPOTIFY_CLIENT_SECRET
  - SPOTIFY_REDIRECT_URI (usa tu dominio de Netlify o Netlify Dev)
- En Spotify Dashboard registra el mismo Redirect URI.
